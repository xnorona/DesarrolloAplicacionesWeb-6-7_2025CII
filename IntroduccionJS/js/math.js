//Objeto Math

let resultado;

resultado = Math.PI //Valor de PI
resultado = Math.round(2.2); //redondea
resultado = Math.ceil(2.2); //redondear hacia arriba
resultado = Math.floor(2.9); //redondear hacia abajo
resultado = Math.sqrt(144); //raiz cuadrada
resultado = Math.abs(-200); //Convierte el numero en positivo
resultado = Math.min(3,5,1,2,4,6); //devuelve el valor minimo de un listado
resultado = Math.max(3,5,7,2,4,6) //devuelve el valor maximo de un listado
resultado = Math.random(); //Genera un valor aleatorino entre 0 y 1
resultado = Math.floor(Math.random() * 30); // combinación de funciones

console.log(resultado);